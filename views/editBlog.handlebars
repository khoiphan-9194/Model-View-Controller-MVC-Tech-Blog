{{#if logged_in}}
<h6>Welcome {{user_name}}</h6>
   <div class="row">
    <div class="text-center mx-auto col-lg-10 col-md-8 col-sm-7">

        
    <form class="edit-post-form">
      <div>
        
        <label for="edit-text">Have a new thought on blog name <h3>{{blog.blog_name}}</h3>????</label>
        <textarea id="edit-text" name="edit-text"></textarea>
      </div>
      <button type="click" class="btn btn-dark mt-3">Save your new thought!</button>
    </form>

    </div>
</div>
{{/if}}
<script src="/js/updateBlog.js"></script>
